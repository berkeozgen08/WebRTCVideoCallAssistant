import { TotalTimePipe } from './total-time.pipe';

describe('TotalTimePipe', () => {
  it('create an instance', () => {
    const pipe = new TotalTimePipe();
    expect(pipe).toBeTruthy();
  });
});
